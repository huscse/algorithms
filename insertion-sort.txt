// pseudocode for insertion sort Algorithm

InsertionSort(Array):
    n = length(Array)
    
    for i = 1 to n - 1:
        key = Array[i]
        j = i - 1

    while j >= 0 && A[j] > key:
        Array[j + 1] = A[j]
        j = j - 1

    A[j + 1] = key